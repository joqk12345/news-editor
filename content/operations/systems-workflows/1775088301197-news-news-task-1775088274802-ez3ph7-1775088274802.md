---
title: "1775088301197-news-news-task-1775088274802-ez3ph7"
description: "1775088301197-news-news-task-1775088274802-ez3ph7"
generatedBy: "reports-pipeline"
generatedType: "report"
tags: [operations, systems-workflows, news, rewrite, ai, agents, models, product]
category: "operations/systems-workflows"
priority: "low"
priorityScore: 4
date: "2026-04-02"
docType: "rewrite"
sourceBucket: "news"
source: "reports/news/2026-04-02/1775088301197-news-news-task-1775088274802-ez3ph7.md"
requestId: "1775088274802-ez3ph7"
---

> 1775088301197-news-news-task-1775088274802-ez3ph7

## 元信息
- 分类：`operations/systems-workflows`
- 优先级：`low` (`4`)
- 文档类型：`rewrite`
- 来源分组：`news`
- 原始文件：`reports/news/2026-04-02/1775088301197-news-news-task-1775088274802-ez3ph7.md`
- 请求 ID：`1775088274802-ez3ph7`

## 原始内容

#### 原文中英重写

#### 中文重写
**结论：** 深入分析 Claude Code 泄露的源码发现，十八种电子宠物动物名称全部采用十六进制编码，这一异常策略强烈暗示其中隐藏着一个被 Anthropic 通过编译时扫描器（`excluded-strings.txt`）严格屏蔽的敏感名称，其保护方式类似于“证人保护计划”。

**依据：**
1.  **事件起源**：2026年3月31日凌晨，Solayer Labs 实习生发现 Anthropic 的 Claude Code npm 包中包含一个 59.8 MB 的 source map 文件，使编译后代码的内部结构完全暴露。该文件迅速被镜像至 GitHub，引发广泛关注和媒体报道，但多数报道仅罗列功能（如 KAIROS、44 个功能开关），未深入机制。
2.  **源码异常**：在 `types.ts` 文件约第 400 行处，所有动物名称（如 `duck`, `goose`, `cat` 等共十八种）均通过 `String.fromCharCode` 与十六进制字节（如 `0x64 0x75 0x63 0x6b`）组合定义，确保明文不直接出现在编译产物中。其上方注释明确指出：“有一个物种名与 `excluded-strings.txt` 中的模型代号冲突。”
3.  **关联系统**：这些动物属于 `/buddy` 电子宠物系统，于泄漏次日（4月1日）上线。用户根据账户 ID 获得随机宠物，具备稀有度（传说概率 1%）、属性（如 DEBUGGING）及帽子等装饰。源码中 `sprites.ts` 文件还包含了这些动物的 ASCII 像素画。
4.  **编码策略解读**：Anthropic 为防范敏感名称泄露，构建了 `excluded-strings.txt` 扫描器，一旦检测到该明文即导致构建失败。然而，源码中对全部十八个物种（包括一个未命名的 `0x63 0x61 0x70 0x79 0x62 0x61 0x72 0x61`）实施了同等强度的十六进制编码，而非仅保护敏感名称。这种“整条街迁走”的过度防护，表明其意图是让敏感名称在大量同类数据中彻底无法被识别，体现了超越常规工程的隐蔽意图。

#### English Rewrite
**Conclusion:** Deep analysis of the leaked Claude Code source code reveals that all eighteen electronic pet animal names are hex-encoded. This extraordinary measure strongly suggests the presence of a single sensitive name that Anthropic actively shields via a compile-time scanner (`excluded-strings.txt`), employing a "witness protection program" strategy to obscure it among the rest.

**Evidence:**
1.  **Origin**: On March 31, 2026, a Solayer Labs intern discovered a 59.8 MB source map file within Anthropic's Claude Code npm package, exposing the internal structure of the compiled code. The file was mirrored to GitHub, sparking media coverage that largely focused on listing features (e.g., KAIROS, 44 feature flags) rather than underlying mechanisms.
2.  **Source Code Anomaly**: In `types.ts` (around line 400), all animal names (e.g., `duck`, `goose`, `cat`, etc., totaling eighteen) are defined via `String.fromCharCode` combined with hex byte sequences (e.g., `0x64 0x75 0x63 0x6b`), preventing plaintext appearance in build outputs. A comment above states: "One species name collides with a model-codename canary in excluded-strings.txt."
3.  **Linked System**: These animals belong to the `/buddy` electronic pet system, launched the day after the leak (April 1). Users receive a deterministic pseudo-random pet based on their account ID, with rarities (legendary at 1%), attributes (e.g., DEBUGGING), and hats. ASCII pixel art for the animals is stored in `sprites.ts`.
4.  **Encoding Strategy**: Anthropic implemented `excluded-strings.txt` to fail builds if the sensitive plaintext string appears. However, the source applies identical hex encoding to all eighteen species—including one unnamed (`0x63 0x61 0x70 0x79 0x62 0x61 0x72 0x61`)—not just the sensitive one. This "move the entire street" over-protection indicates a deliberate effort to make the sensitive name indistinguishable within a large set of similar obfuscated data, reflecting an intent beyond standard engineering practices.
