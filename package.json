{
  "name": "news-editor",
  "private": true,
  "type": "module",
  "scripts": {
    "sync:content": "python3 scripts/organize-reports.py && python3 scripts/generate-index.py && python3 scripts/validate-content.py",
    "validate:content": "python3 scripts/validate-content.py",
    "dev": "vitepress dev",
    "build": "vitepress build",
    "preview": "vitepress preview"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  }
}
